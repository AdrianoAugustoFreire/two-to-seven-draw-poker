import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Two to Seven Draw Poker',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
